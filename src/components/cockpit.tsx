import { h, Component } from "preact"
import "./cockpit.scss"

export default class Cockpit extends Component<{}, {}> {
    public render() {
        return <div class="cockpit">Cockpit</div>
    }
}
